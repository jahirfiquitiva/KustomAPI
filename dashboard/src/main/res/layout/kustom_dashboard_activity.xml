<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_kustom_dash_board"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="org.kustom.api.dashboard.DashboardActivity">

    <Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:background="?android:attr/colorPrimary"
        android:elevation="4dp" />

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:background="?android:attr/colorPrimary"
        android:elevation="4dp"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        app:pstsShouldExpand="true"
        app:pstsTabTextSize="14dp"
        app:pstsIndicatorColor="?android:attr/colorAccent"
        app:pstsTabTextColor="?android:attr/textColorPrimary"
        app:pstsIndicatorHeight="3dp"
        app:pstsUnderlineColor="?android:attr/textColorSecondaryInverse" />

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:context=".MainActivity" />

</LinearLayout>
