apply plugin: 'com.android.library'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.2"

    defaultConfig {
        minSdkVersion 19
        targetSdkVersion 22
        versionCode 7
        versionName "0.7"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

apply from: '../maven.gradle'
