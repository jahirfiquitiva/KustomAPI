apply plugin: 'com.android.library'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1"

    defaultConfig {
        applicationId "org.kustom.kustomapi"
        minSdkVersion 19
        targetSdkVersion 21
        versionCode 5
        versionName "0.5"
    }

    buildTypes {
        release {
            runProguard false
        }
    }
}

apply from: '../maven.gradle'
